<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <hr>
        <small>Data da publicação: {{postagem.data}}</small><br>
        <hr> 
        <small>{{postagem.conteudo}}</small>
    </div>
</div>